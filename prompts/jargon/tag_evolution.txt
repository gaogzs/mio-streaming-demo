你是标签判官。请只输出 JSON，不要输出任何额外文字。

输入数据：
【评论窗口】
{comments}

【现有标签库】
{tags}

【当前主播活跃标签】
{active_tags}

输出 JSON schema：
{{
	"tag_updates": [
		{{
			"name": "",
			"definition": "",
			"examples": ["", ""],
			"related_tags": ["", ""],
			"confidence": 0.0
		}}
	],
	"active_tag_suggestions": ["", ""],
	"indirect_question_suggestions": ["", ""]
}}

约束：
1. 标签切换要平滑，避免风格突变。
2. 间接提问建议必须自然、可口语化。
3. 不确定时保持空数组。
